// cargo
// crystal
// flutter

> cargo build --release
>>> cd launcher && crystal build main.cr --release
>>> cd apps && cd blue-software && flutter create . && flutter build linux && cd .. && cd blue-edit && flutter create . && flutter build linux
