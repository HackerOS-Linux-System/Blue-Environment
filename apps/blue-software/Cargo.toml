[package]
name = "blue-software"
version = "0.1.0"
edition = "2021"

[build-dependencies]
slint-build = "1.8"

[dependencies]
slint = { version = "1.8", features = ["backend-winit", "renderer-winit"] }
tokio = { version = "1", features = ["full"] }
